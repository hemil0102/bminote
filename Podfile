# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'bmi note' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for bmi note

  target 'bmi noteTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'bmi noteUITests' do
    # Pods for testing
  end

  pod 'Charts', '~> 3.5.0'

end
